server:
  port: 9002

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/kumud2795/user-api-cloud-configuration.git
          clone-on-start: true